require('./../../spec_helper');
const t         = require('track-spec');
const ViewModel = require('./../../../app/view_models/components/switch');

t.describe('Components::Switch', () => {
  let vm = null;

  t.before(() => {
    vm = new ViewModel();
  });

  console.warn('[PENDING] Please write Switch test in `tests/view_models/components/switch`');
});
