require('./../../spec_helper');
const t         = require('track-spec');
const ViewModel = require('./../../../app/view_models/controllers/switches');

t.describe('Controllers::Switches', () => {
  let vm = null;

  t.before(() => {
    vm = new ViewModel();
  });

  console.warn('[PENDING] Please write Switches test in `tests/view_models/controllers/switches`');
});
