require('./../../spec_helper');
const t         = require('track-spec');
const ViewModel = require('./../../../app/view_models/controllers/counter');

t.describe('Controllers::Counter', () => {
  let vm = null;

  t.before(() => {
    vm = new ViewModel();
  });

  console.warn('[PENDING] Please write Counter test in `tests/view_models/controllers/counter`');
});
